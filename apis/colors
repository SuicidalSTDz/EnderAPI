colors.isValid = function( nColor )
  assert(type(nColor)=="number","number expected, got "..type(nColor))
  
  for _, v in pairs( colors ) do
    if nColor == v then
      return true
    end
  end
  return false
end
colours.isValid = colors.isValid
