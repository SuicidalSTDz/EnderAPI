colours.isValid = function( nColour )
  assert(type(nColour)=="number","number expected, got "..type(nColour))
  
  for _, v in pairs( colours ) do
    if nColour == v then
      return true
    end
  end
  return false
end
