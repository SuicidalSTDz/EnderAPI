local version = 0.1
local showUpdateStatus = true
local shouldAutoUpdate = true

function returnVars()
  return {
    version = version,
    showUpdateStatus = showUpdateStatus,
    shouldAutoUpdate = shouldAutoUpdate
    }
end
